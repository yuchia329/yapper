"""Generated gRPC stubs + protos shared by the orchestrator (clients) and the GPU
services (servers). Minimal deps (grpcio, protobuf) so it installs on both the web
image and the GPU box without pulling the heavy pipeline dependencies.

Regenerate after editing a .proto:  bash scripts/gen_protos.sh
"""
